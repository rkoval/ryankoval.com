[install]
# 6-month supply-chain guard: skip package versions published less than 180 days ago.
minimumReleaseAge = 15552000
# Each entry bypasses the guard for one package — confirm with the user
# before adding any.
minimumReleaseAgeExcludes = ["@lovable.dev/vite-tanstack-config", "@lovable.dev/mcp-js"]
